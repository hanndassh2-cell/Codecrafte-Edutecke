# المهمة الحالية — إظهار أدوات قائمة MathLive ضمن شريط المعادلة

## المرحلة 2026-09-25: خيارات الزر الأيمن
- [x] فُحصت صور القوائم السبع وواجهة `MathLive 0.110` المثبتة، ثم أُضيفت أدواتها إلى نافذة `EquationEditorModal` المستخدمة بالفعل.
- [x] معرض التراكيب يعرض الآن اللوغاريتم، مشتقات عند نقطة ومن الرتبة n، والأعداد المركبة (الطويل، السعة، الحقيقي، التخيلي، المرافق). شبكة مصفوفات 1×1 إلى 5×5 تستخدم أمر MathLive الأصلي.
- [x] صف صغير ضمن شريط النافذة للوضع (رياضيات/نص/LaTeX)، أنماط الخط، اللون والتظليل، والتحرير (قص، نسخ بصيغ LaTeX/Typst/AsciiMath/MathML، لصق، تحديد الكل). الأوامر تستدعي عناصر قائمة MathLive المثبتة نفسها، مع استعادة التحديد قبل التنفيذ. قائمة الزر الأيمن تبقى اختصارًا إضافيًا.
- [x] اختبار تفاعلي على MathLive الحقيقي: مصفوفة 4×3، قوالب جديدة، تحديد ثم غامق/لون/تظليل، تبديل الوضع وتحديد الكل. اختبارات الإدراج والتعديل السابقة E01–E07 لا تزال ضمن السلسلة.
- [x] `npm run lint` (TypeScript)، `npm test` كاملًا، `npm run build`، و`git diff --check` نجحت. يبقى تحذير Vite لحجم الحزمة وتحذير TipTap لتكرار اسمي link/underline كما في المرحلة السابقة.
- [ ] اختبار بصري فعلي على المتصفح للشاشات المختلفة والحافظة ضمن سياق HTTPS؛ واجهة الفحص السابقة رفضت معاينة المتصفح بـ `ERR_BLOCKED_BY_CLIENT`. لا تُعد لقطة فعلية مُتحققة.
- [ ] اختبار طباعة وPDF بصريًا بقي مهمًا قبل اعتماد التطابق النهائي؛ هذه المرحلة لم تغير مسارات الحفظ والتصدير.

الملفات المباشرة: `EquationEditorModal.tsx`، `equation-editor.css`، `equationEditorModal.test.tsx`، وهذا السجل. رابط النشر هو الرابط الحالي نفسه بعد نجاح نشر النسخة الجديدة.

## المرحلة السابقة — إعادة تصميم نافذة المعادلات

## نقطة الاستئناف
- الموقع: `appgprj_6aa554a29e0881918b84b1be8bc52c85`، فرع الموقع `main`.
- البداية: إصدار 47، commit `5a878ab6215ec68c3f10be5f5bb20916d0d1c263`، شجرة نظيفة.
- فُحصت ملفات السياق والمسار الفعلي: `RichTextEditor → EquationEditorModal` لإدراج/تعديل معادلة داخل النص، و`EditorCard → EquationEditorModal` لبطاقة المعادلة.
- لا محرر موازٍ ولا استبدال MathLive/TipTap. لم تتغير بيانات الدرس أو الحفظ أو التخزين أو مسارا PDF.

## المنفذ
- [x] النافذة نفسها أصبحت عريضة RTL؛ رأس مختصر، تبويبا رياضيات/كيمياء، شريط بصري من تسع مجموعات ومعرض في تدفق الصفحة دون تغطية حقل الكتابة.
- [x] كسر، أس ودليل، جذر، تكامل، مجموع، نهاية، أقواس، مصفوفة، رموز. قوالب جديدة بخانات فارغة، مع المحافظة على القوالب القديمة ومكتبتها الكاملة ضمن الخيارات المتقدمة.
- [x] حقل MathLive واحد LTR؛ إدراج القالب عند مؤشره واختيار أول خانة باستخدام API القائم.
- [x] الكيمياء تستخدم أسهم التفاعل والاتزان والغاز والراسب والأسس والأدلة والنظير، دون ادعاء موازنة تلقائية.
- [x] خيارات متقدمة مطوية تضم Word/LaTeX/MathML، نسخ/مسح الكود، الإصلاح والسجل والمكتبة السابقة.
- [x] لوحة المفاتيح اختيارية؛ ضبط طبقتها مؤقتًا عند فتح النافذة وإتاحة مساحة لها وفق ارتفاعها، واستعادة الإعداد السابق عند الإغلاق.
- [x] ضمن السطر/سطر مستقل، وزر تحديث عند فتح معادلة موجودة. منطق التغليف بـ $/$$ والأوامر الأصلية للإدراج والاستبدال باقية.
- [x] منع المعادلة الفارغة، منع النقر المكرر أثناء الإدراج، إبقاء المسودة والنافذة عند نتيجة false أو رفض/استثناء.
- [x] حفظ تحديد البطاقة قبل الفتح واستعادته عند الإلغاء؛ مسح المعادلة السابقة عند طلب إدراج جديد. عدم إعادة الإدراج كنسخة أخرى عند فشل تعديل المعادلة.
- [x] CSS محصور بالنافذة، معرض يلتف، ومحتوى قابل للتمرير للشاشات القصيرة. لا تغيير لـ CSS محتوى المستند أو الإخراج.

## المختبر فعليًا
اختبار `equationEditorModal.test.tsx` يشغّل React وMathLive المثبت الحقيقي وRichTextEditor الحقيقي داخل JSDOM. بدائل البيئة تخص قياسات/تمرير DOM والخطوط فقط، وليس محرّك المعادلات أو أوامر الإدراج.
- [x] E01: فتح المعادلة الحالية بقيمتها الصحيحة، حقل واحد، الخيارات مطوية، والإلغاء.
- [x] E02: منع الفراغ، إدراج كسر وملء البسط والمقام والتنقل بين خانات MathLive، وحفظ block.
- [x] E03: الكسور المركبة والأسس والجذور والتكامل والمجموع والنهايات والأقواس والمصفوفات 2×2 و3×3، صحة تحليلها وتحديد أول خانة؛ الإدراج وسط تعبير والكيمياء وإتاحة المكتبة السابقة.
- [x] E04: فشل/رفض الإدراج يبقي الكتابة، ومنع تكرار العملية أثناء الانتظار.
- [x] E05: الإصلاح وسجل النسخ والاستعادة وEscape.
- [x] E06: إدراج inline في البطاقة عند الموضع المحفوظ، تعديل دون تكرار، تراجع/إعادة، إدراج جديد فارغ بعد تعديل سابق، وإلغاء يعيد التحديد دون تغيير المستند.
- [x] E07: المحتوى المحفوظ يعرض عبر MathText/KaTeX الحالي دون خطأ تشغيل.
- [x] `npm test` كامل ناجح (يشمل الاختبار الجديد)، واختبارات المعاينة ودورة الدرس والمناهج والقوائم والجداول والأمان القائمة.
- [x] TypeScript (`npm run lint` = `tsc --noEmit`) ناجح؛ لا يوجد ESLint مستقل.
- [x] `git diff --check` ناجح.

## المتبقي وحدود التحقق
- [ ] لقطة فعلية ومقارنة بصرية Desktop/Tablet/Mobile: المعاينة تعمل، لكن متصفح الفحص رفض عنوانها المخصص بـ `ERR_BLOCKED_BY_CLIENT`. لا لقطة مولدة بديلة ولا ادعاء اختبار بصري.
- [ ] تحقق متصفح من لوحة المفاتيح الافتراضية والطبقات والتمرير والتنقل الفيزيائي بالأسهم/Tab؛ الاختبار البرمجي تحقق من أوامر الخانات لا الرسم أو هندسة النقر.
- [ ] فحص معاينة الدرس الشاملة والطباعة وملفي PDF بصريًا. المحركات لم تتغير؛ اختبار MathText لا يثبت مطابقة PDF.
- التحذيرات السابقة: link/underline مكرران في اختبارات TipTap، ورسائل مقاييس الحروف العربية لقالب الحالات في MathLive تحت بيئة اختبار دون خطوط فعلية.

## الملفات
- `src/modules/editor/components/EquationEditorModal.tsx`
- `src/modules/editor/components/equation-editor.css`
- `src/modules/editor/components/RichTextEditor.tsx`
- `src/tests/equationEditorModal.test.tsx`
- `package.json`: إضافة الاختبار إلى سلسلة npm test فقط؛ لا تغيير تبعيات أو lockfile.
- `CURRENT_TASK.md`

## خارج هذه المرحلة
- طلب إعادة تصميم «إضافة بطاقة» سبق فحصه فقط ولم يُنفذ؛ انتقل المستخدم إلى نافذة المعادلات.
- المخطط القديم ما زال محفوظًا حتى التحقق البصري من تكافؤ وظائف المساحة الموحدة.
- آخر بطاقة محفوظة في ذاكرة جلسة التطبيق فقط كما في المرحلة السابقة.

## البناء والنشر
- [x] `npm run build` ناجح؛ تحذير حجم حزم Vite السابق باقٍ.
- التسليم عبر نشر الرابط نفسه مع إبقاء جمهور الموقع خاصًا بالمالك؛ نتيجة النشر النهائية في سجل الموقع.
- يعتمد الالتزام النهائي ورقم الإصدار على سجل Git ونتيجة نشر الموقع، لا على أرقام متوقعة مسبقًا.
