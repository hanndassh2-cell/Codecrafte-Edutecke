# EduTech — AI Project Context

> **الغرض:** مرجع شبه ثابت لبنية المشروع وقراراته. اقرأ بعده `AI_RULES.md` ثم `CURRENT_TASK.md` قبل أي تعديل.  
> **آخر تحقق:** 2026-09-18 — على نقطة الأساس الجديدة بعد نقل المشروع إلى VS Code.  
> **آخر إصدار ملتزم وموثوق:** `c3397a8` (`Baseline - EduTech latest before AI agents`)، الفرع `edutech-latest-2026-09-18` يتتبع `origin/edutech-latest-2026-09-18`.  
> **المعاينة المنشورة عند آخر تسليم:** الإصدار 33 على `https://edutech-curriculum-audit-preview.hanndassh2.chatgpt.site` (يحتاج التحقق إن كانت النشرة الحالية ما زالت نفسها بعد هذا الـ baseline).
> **ملاحظة:** سجل الالتزامات المفصل في القسم 4 أدناه يعود لمرحلة سابقة على هذا الـ baseline؛ يحتاج تحققًا من `git log` الفعلي قبل الاعتماد عليه كسجل دقيق للحاضر.

## 1. الهدف العام

EduTech نظام عربي لإدارة المحتوى الأكاديمي وبناء الدروس والأسئلة والاختبارات، مع تحرير غني للغة العربية والرياضيات، معاينة A4، وطباعة/تصدير. المشكلة الأساسية التي يحلها هي تحويل إعداد الدرس من صفحة مزدحمة ببطاقات ومحررات متكررة إلى تدفق واضح وآمن:

`المنهاج ← الدرس ← مساحة إعداد موحدة (المحرر والمخطط الجانبي) ← المعاينة ← التصدير/الطباعة`

يركز المشروع على:

- إدخال يومي سريع للأستاذ دون فقد بيانات أو تعديل الدرس الخطأ.
- دعم RTL والعربية والمحتوى المختلط عربي/إنجليزي/رياضيات.
- استيراد محتوى Word/PDF/HTML/TXT وOCR، وإدراج صور وجداول ومعادلات.
- مساحة إعداد واحدة، بمخطط جانبي قابل للتوسيع ومحرر مركز للبطاقة النشطة.
- إخراج مطابق للمعاينة، مع مسار PDF بصري ومسار PDF نص حي مستقل.

## 2. بنية البرنامج ومساراته الرئيسية

التطبيق React أحادي الصفحة، وتحدد `src/App.tsx` التنقل والحالة العامة والصلاحيات. الوحدات الظاهرة فعليًا تشمل:

- لوحة التحكم.
- مستكشف/شجرة المناهج: مواد ← وحدات ← دروس.
- إعداد الدروس ومخطط الدرس ومحرر البطاقة.
- بنك الأسئلة.
- توليد الاختبارات ومكتبة الاختبارات والدورات السابقة.
- المستخدمون والصلاحيات، الإعدادات، النسخ الاحتياطي، والذكاء الاصطناعي.

المسار المعتمد لإعداد الدرس:

1. يفتح المستخدم **مستكشف المناهج**.
2. يختار المادة ثم الوحدة ثم الدرس، ويضغط **تحرير الدرس**.
3. يفتح **إعداد الدرس** المحرر الحالي مباشرة على آخر بطاقة خلال جلسة التطبيق، أو الأولى؛ الدرس الفارغ يعرض «إضافة أول بطاقة».
4. المخطط الجانبي داخل مساحة العمل يتيح البحث والتصفية والإضافة والترتيب من المقبض أو لوحة المفاتيح والإخفاء والتكرار والحذف.
5. «توسيع المخطط» يوسّع مكوّن المخطط نفسه ولا يفك تركيب المحرر؛ العودة تستعيد التحديد والتمرير. يبقى «المخطط السابق» في المزيد حتى اكتمال التحقق البصري من التكافؤ.
6. الحفظ، الإغلاق، السابق، والتالي تنتظر نتيجة الحفظ؛ عند الفشل يبقى المحرر مفتوحًا.
7. **المعاينة** تجهز أحدث محتوى ثم تفتح معاينة الدرس كاملة.
8. من المعاينة: طباعة، PDF كصورة، PDF نص حي، وPNG بحسب الخيارات المتاحة.

## 3. التقنيات والمكتبات المستخدمة فعليًا

| المجال | التقنية الفعلية |
|---|---|
| الواجهة | React 19، TypeScript 5.8، Vite 6، Tailwind CSS 4، `lucide-react`، `motion` |
| الخادم/البناء | Express 4، esbuild، `tsx`، `dotenv` |
| المحرر | TipTap 3 / ProseMirror مع StarterKit وامتدادات النص والروابط والصور والجداول والمحاذاة والألوان والتظليل والخطوط |
| الرياضيات | MathLive 0.110، KaTeX 0.18، وامتدادات TipTap مخصصة |
| الاستيراد | `mammoth` لـ DOCX، `pdfjs-dist` و`pdf-parse` لـ PDF، HTML/TXT، ومسار OCR موجود في التطبيق |
| التصدير | `jsPDF`، `html-to-image`، `html2canvas`، `html2pdf.js` |
| الخطوط | Cairo وAmiri، مع ملفات TTF عامة لمسار PDF النصي |
| الاختبارات | ملفات TypeScript تعمل عبر `node --import tsx`، إضافة إلى JSDOM وCanvas عند الحاجة |
| الذكاء الاصطناعي | `@google/genai` وخدمات/مهايئات داخلية؛ الإعداد الفعلي يحتاج تحقق قبل التغيير |

يوجد `prisma/schema.prisma` و`electron-integration/schema.prisma` ومسار Electron، لكن **لا تفترض أن Prisma هي قاعدة تشغيل نسخة الويب الحالية**؛ التشغيل الحالي المؤكد للويب يعتمد على التخزين المحلي وخدمات المستودعات الموضحة أدناه.

## 4. ما تم تطويره في هذه الدورة، بالترتيب

الترتيب التالي مستخلص من سجل Git وسياق العمل:

1. إصلاح فراغات اللصق المتكررة: طي فواصل HTML، إخفاء فجوات المحرر، توحيد لصق لوحة المفاتيح/القائمة السياقية، ثم تطبيع المحتوى بعد تحديث TipTap (`2ab14b8` إلى `cc30068`).
2. تحسين دورة إضافة البطاقة: إظهار حالة الحفظ الصحيحة، تركيز البطاقة الجديدة، والتنقل بين البطاقات (`8d62be0` إلى `80de1b0`).
3. تبسيط تدفق إعداد الدرس وإعادة تصميم سطح الكتابة وأدوات التنسيق ليقترب من Google Docs (`0153c3c` إلى `652725f`).
4. إضافة مساحة كتابة مركزة للبطاقة، ثم مساحة منظمة للدروس الطويلة (`30ded5f`, `64bf269`, `b098a08`).
5. إعادة هيكلة إعداد الدرس حول المخطط: إزالة تكرار المحررات، فتح البطاقة في محرر مستقل، ومعاينة كاملة بدل المعاينة الجانبية (`723b8fc`, `96403ae`, `01ef608`).
6. إصلاح أمان النص العربي في التصدير، ثم إعادة مسار PDF النص الحي كمسار مستقل عن PDF الصورة (`ba54f82`, `61d16b6`).
7. تحويل واجهة المناهج إلى **مستكشف تدريجي** مادة ← وحدة ← درس، بدل شجرة جانبية مكتظة (`f7a07fd`).
8. إضافة مؤشرات جاهزية البطاقات وتحسين مخطط الدرس (`a4c29d8`).
9. توحيد الاستيراد الآمن وإضافة DOCX/PDF/HTML/TXT مع مراجعة قبل الإدراج أو الاستبدال، وتحويل PDF المصور إلى OCR (`e823bef`, `4a21ade`).
10. إضافة أدوات سياقية للجداول والصور (`163b193`).
11. جعل دورة المحرر **معاملية**: لا بديل تلقائي للدرس الأول، الحفظ يعيد نجاح/فشل قابلًا للانتظار، الإغلاق والتنقل ينتظران الحفظ، والمعاينة/التصدير تلتقط أحدث محتوى (`d185479`).
12. **العمل الحالي غير الملتزم:** إعادة تنظيم شريط الإدراج إلى مجموعات Word-style، وتحسين تدفق نافذة المعادلات إلى: اختر البنية ← اكتب/عدّل ← راجع/أدرج. نجح `lint` والاختبار والبناء، لكنه لم يُفحص بصريًا ولم يُلتزم أو يُنشر بعد.

## 5. القرارات التصميمية والبرمجية المعتمدة

- **المنهاج هو نقطة الدخول الوحيدة لتحرير الدرس:** يمنع ازدواج السياق وفتح درس غير مقصود.
- **لا شجرة منهاج داخل إعداد الدرس:** الصفحة مخصصة لبناء الدرس لا للتنقل العام.
- **مخطط الدرس هو مصدر التنظيم:** البطاقات تظهر كصفوف مختصرة، لا كمحررات كاملة متتالية.
- **محرر بطاقة مركز:** يقلل الازدحام ويعطي مساحة كتابة تشبه المستند.
- **معاينة كاملة عند الطلب:** لا معاينة جانبية دائمة تستهلك مساحة التحرير.
- **الإفصاح التدريجي:** الأدوات اليومية ظاهرة، والمتقدمة داخل قوائم/أشرطة سياقية.
- **الحفظ قبل الانتقال:** الإغلاق والسابق/التالي والمعاينة لا تتجاوز فشل الحفظ.
- **لا fallback لدرس آخر:** إذا كان `lessonId` غير صالح، تعرض حالة اختيار درس بدل تعديل أول درس تلقائيًا.
- **مسارا PDF مستقلان:** PDF الصورة للدقة البصرية، وPDF النص الحي للبحث والنسخ. يمنع دمجهما في تنفيذ واحد.
- **عدم كسر البيانات القديمة:** التطوير تراكمي مع تطبيع وترحيلات ونسخ إنقاذ، وليس إعادة بناء مدمرة.
- **المعادلات أصل قوي قائم:** لا تعِد بناء محرك المعادلات؛ حسّن الترتيب والتدفق فقط ما لم يثبت خلل جوهري.

## 6. حالة الأجزاء

| الجزء | الحالة | ملاحظات |
|---|---|---|
| مستكشف المناهج التدريجي | مكتمل وظيفيًا | مادة ← وحدة ← درس، مع تحرير الدرس صراحةً |
| فتح الدرس الصحيح | مكتمل ومحمي | أزيل fallback إلى أول درس؛ يوجد اختبار دورة حياة |
| مخطط الدرس | مكتمل وظيفيًا | إضافة، تحرير، معاينة، ترتيب، نسخ، إخفاء، حذف؛ يلزم استمرار فحص UX مع أعداد كبيرة |
| محرر البطاقة المركز | مكتمل كأساس قوي | أهم جزء؛ يحتاج مواصلة صقل Word/Docs بصريًا ووظيفيًا |
| الحفظ والتنقل المعاملي | مكتمل | يبقى المحرر مفتوحًا عند فشل الحفظ |
| تنسيق النص والقوائم وRTL | يعمل | يحتاج مصفوفة فحص أوسع للمحتوى المختلط والحالات الحدية |
| لصق Word/الويب | يعمل جزئيًا | توجد معالجة وتنظيف؛ مشكلة الأسطر الفارغة من لصق الزر الأيمن لم تُغلق بصريًا 100% |
| استيراد DOCX/PDF/HTML/TXT | مكتمل وظيفيًا | مراجعة ثم إدراج/استبدال؛ PDF المصور يوجه إلى OCR |
| OCR | مدمج | يحتاج اختبار طرفي شامل للنص العربي والمعادلات والصور الممسوحة |
| المعادلات | ناجح وظيفيًا | محرك MathLive/KaTeX قائم؛ إعادة تنظيم الواجهة الحالية غير ملتزمة وتحتاج فحصًا بصريًا |
| الجداول | مكتمل وظيفيًا | إدراج 3×3 وأدوات سياقية للتعديل؛ حالات الدمج/التصدير تحتاج تحقق مستمر |
| الصور | مكتمل وظيفيًا | ضغط وإدراج Base64 وأدوات سياقية؛ راقب حصة التخزين وجودة الطباعة |
| المعاينة A4 | تعمل | تجهيز موحد لأحدث المحتوى قبل الفتح |
| PDF كصورة | يعمل/المسار المعتمد | أعلى تطابق بصري، لكنه غير قابل للبحث كنص |
| PDF نص حي V2 | منفذ ومستقل | اختبارات المسار ناجحة؛ ما زال موسومًا تجريبيًا ويحتاج فحص العربية بصريًا ونسخًا/بحثًا |
| الطباعة | تعمل برمجيًا | تستخدم `window.print()`؛ حدثت لقطة سابقة لمعاينة طباعة فارغة، لذا يلزم إعادة اختبار متصفح/طابعة حقيقي |
| اختبارات وأمان RBAC | تعمل | جميع الاختبارات الحالية ناجحة في 2026-09-18 |
| تحسين حجم حزم الإنتاج | يحتاج تحسين | Vite يحذر من chunks كبيرة؛ ليس عطلًا وظيفيًا |

## 7. محرر البطاقة — مربط الفرس

المحرر مبني في الأساس على `RichTextEditor.tsx` و`EditorPanel.tsx`، ويستخدم TipTap/ProseMirror. هدفه أن يشعر الأستاذ بأنه يعمل داخل مستند، مع إبقاء العمليات التخصصية بسيطة.

### بنية الواجهة

- رأس البطاقة: النوع، العنوان، الحالة، وأوامر البطاقة.
- شريط تنسيق أساسي: نمط الفقرة، الخط، الحجم، غامق/مائل/تحته خط، لون/تظليل، محاذاة، اتجاه، وقوائم.
- شريط إدراج مستقل قابل للفتح، حتى لا تختلط أدوات الإدراج بتنسيق النص.
- مساحة كتابة RTL تلقائي مع دعم المقاطع الرياضية LTR.
- أدوات سياقية تظهر عند تحديد جدول أو صورة.
- مخطط مصغر للبطاقات والتنقل السابق/التالي في وضع التحرير المركز.

### التنسيق والمدخلات المدعومة

- فقرات وعناوين، خطوط وأحجام، لون نص وخلفية/تمييز، محاذاة، تباعد، مسافة بادئة، وارتفاع سطر.
- قوائم نقطية ورقمية وأنماط قوائم مخصصة.
- RTL/LTR تلقائي عبر `AutoMathDirection` و`MixedBidiPipeline` وخدمة `bidiContentPipeline`.
- روابط، صور، جداول، مرتفع/منخفض، ومعادلات inline أو block.
- نسخ/قص/لصق/تحديد الكل/تراجع/إعادة عبر أدوات المحرر والمتصفح.

### الإدراج

المسار المنشور الحالي يحتوي ملفًا، معادلة، جدولًا، صورة، OCR، ومساعدًا ذكيًا. التعديل غير الملتزم يعيد ترتيبها إلى:

- **عناصر الدرس:** معادلة، جدول، صورة.
- **استيراد واستخراج:** استيراد ملف، استخراج OCR.
- **مساعدة:** المساعد الذكي.

لا تضف عشرات الأزرار إلى الصف الأساسي. أي ميزة جديدة يجب أن تدخل في المجموعة المناسبة أو شريط سياقي.

### المعادلات

- المحرك: MathLive للإدخال وKaTeX للعرض/المعاينة.
- يدعم كسورًا وجذورًا، أسسًا وأدلة، أقواسًا ومصفوفات، معاملات، رموزًا وأسهمًا، وقوالب متقدمة.
- يدعم inline وblock، وسجل تعديلات، وإصلاحًا تلقائيًا، وتحرير LaTeX/MathML.
- التعديل الحالي يضيف توافقًا أوضح مع لصق Word، خطوات مرقمة، واختيار فئة ثابت بدل إغلاقها بالنقر الثاني.
- المطلوب التالي هو **فحص بصري وتفاعلي**، لا إعادة كتابة المحرك.

### الصور والجداول

- الصور تضغط عبر `src/services/imageCompression.ts` ثم تدرج كـ Base64.
- `ImageNodeView.tsx` وأدوات الصورة مسؤولة عن التفاعل والحجم/المحاذاة بحسب المتاح.
- الجداول قابلة لتغيير الحجم، ولها صف/خلية رأس وامتدادات TipTap مخصصة وأدوات سياقية.
- يجب اختبار الجداول والصور في المحرر والمعاينة وكلا مساري PDF والطباعة.

### اللصق والاستيراد

- `smartPasteEngine.ts` يحلل HTML والنص والملفات، ينظف شوائب Word، يحول صيغًا رياضية، ويحافظ قدر الإمكان على العناوين والقوائم والجداول والصور.
- اللصق متعدد الأسطر/HTML يمر بالتنظيف ثم `sanitizeHtmlForProseMirror`؛ النص البسيط أحادي السطر يترك للمحرر الأصلي.
- بعد الإدراج يجري تطبيع لاحق لإزالة آثار الفراغات.
- لا تعتبر مشكلة لصق زر الفأرة الأيمن مغلقة حتى تجرّب عينات Word وصفحات ويب و`Ctrl+V` وتقارنها.
- `DocumentImportModal.tsx` يدعم DOCX/PDF/HTML/TXT ويقدم معاينة/مراجعة قبل الإدراج أو الاستبدال. PDF المصور يفتح OCR.

### الاختصارات المؤكدة

| الاختصار | الوظيفة |
|---|---|
| `Ctrl/Cmd + S` | حفظ الدرس/البطاقة |
| `Ctrl/Cmd + P` | فتح الطباعة |
| `Ctrl/Cmd + Shift + N` | إضافة بطاقة جديدة عند توفر الصلاحية |
| `Esc` | إغلاق محرر البطاقة بعد انتظار الحفظ |
| `Arrow Up` عند بداية البطاقة | الانتقال إلى البطاقة السابقة إن أمكن |
| `Arrow Down` عند نهاية البطاقة | الانتقال إلى البطاقة التالية إن أمكن |

اختصارات التحرير القياسية يوفرها المتصفح/TipTap، لكن لا توثق اختصارًا إضافيًا دون اختبار فعلي.

## 8. المعاينة والتصدير والطباعة

### PDF كصورة — المسار المعتمد

- التنفيذ: `src/utils/pdfExporter.ts`.
- يستخدم DOM clone مع `html-to-image` ثم fallback إلى `html2canvas` ويدرج الصفحات في `jsPDF`.
- يحافظ على الشكل العربي والمعادلات بأعلى ثبات.
- متعمد أن يكون image-only؛ لا تضف طبقة نص خفية لأنها قد تعكس ترتيب العربية.

### PDF نص حي V2 — مسار مستقل

- التنفيذ: `src/utils/pdfV2Exporter.ts`.
- يستخدم `jsPDF` وخطوط Amiri/Cairo TTF ونصًا/متجهات قابلة للبحث والنسخ.
- أعيد بعد أن كان ناجحًا سابقًا، وفُصل برمجيًا عن PDF الصورة.
- اختبار الاستقلال ناجح، لكن الواجهة ما زالت تسميه **تجريبيًا**. يجب التحقق يدويًا من تشكيل العربية، ترتيب RTL، النسخ، البحث، المعادلات، وتعدد الصفحات قبل ترقيته إلى معتمد.

### الحاجز المشترك والطباعة

- `src/utils/exportPreflightGate.ts` يجهز الصفحات ويتحقق من الصور/المعادلات وعدد الصفحات قبل التصدير.
- `PrintPreviewModal.tsx` و`UnifiedPreviewToolbar.tsx` ينظمان المعاينة والأوامر.
- الطباعة تستدعي `window.print()`؛ يجب اختبارها على Chrome/Edge مع A4 عمودي/أفقي. توجد ملاحظة سابقة عن شاشة طباعة فارغة لم يُثبت إغلاقها نهائيًا.

## 9. البيانات وقاعدة التخزين والمحافظة على القديم

### مصدر الحقيقة الحالي لنسخة الويب

- `src/services/storage.ts` يستخدم `localStorage` بمفاتيح ذات إصدار مثل `edutech_lessons_v1` و`edutech_lesson_cards_v1`.
- `src/repositories/` يقدم طبقة مستودعات ومحولات `LocalStorageAdapters` بدل ربط الواجهات مباشرة بتفاصيل التخزين.
- البطاقات تحفظ مطبعة حسب `lessonId` عبر `saveLessonNormalized`، مع fallback قراءة من `lesson.contentParagraphs` للتوافق القديم.
- بنك الأسئلة يعمل كمصدر حقيقة واحد عند تفعيل `ENABLE_DATA_2_SSOT_MODE`، مع الاحتفاظ بالمعرف والبيانات السياقية في المراجع.
- إصدار المخطط الحالي `2`، والترحيلات متسلسلة وidempotent وتحفظ الحقول المعروفة وغير المعروفة ثم تتحقق من الكتابة.

### آليات الحماية

- نسخ `SAFE_BACKUP_` للمفاتيح الحرجة: الأسئلة، بطاقات الدروس، والاختبارات، مع قواعد خاصة لمكتبة الاختبارات.
- نقطة إنقاذ وrollback كامل عند فشل الترحيل أو الاستيراد.
- تصدير قاعدة كاملة JSON بصيغة 3 وتوقيع checksum، مع استيراد متوافق مع صيغ أقدم.
- عند امتلاء الحصة يجري تنظيف بيانات غير حرجة مع إبقاء نسخة الذاكرة؛ الصور Base64 قد تكون سببًا رئيسيًا لاستهلاك الحصة.

### قاعدة إلزامية

لا تغيّر أسماء المفاتيح أو `Lesson/Card/Question` shape أو إصدار المخطط أو منطق SSOT مباشرة. أي تغيير بيانات يحتاج: ترحيل idempotent، نقطة إنقاذ، تحقق كتابة، rollback، توافق قديم، واختبارات. وجود Prisma لا يبرر نقل نسخة الويب إليه دون مشروع ترحيل معتمد.

## 10. المشكلات التي أصلحت

- منع فتح/تعديل أول درس عند فقدان معرف الدرس.
- منع الإغلاق أو السابق/التالي عند فشل حفظ البطاقة.
- تمرير أحدث محتوى إلى المعاينة والتصدير قبل فتحهما.
- تقليل ازدحام إعداد الدرس بإلغاء المحررات المتكررة والمعاينة الجانبية.
- توحيد مسار تحرير البطاقة وإضافة بطاقة جديدة وتركيزها.
- تحسين اللصق وتطبيع فراغات HTML عبر عدة طبقات.
- نقل قوائم المحرر فوق المستند لتجنب ظهورها خلف العناصر.
- فصل PDF النصي عن PDF الصورة وإصلاح مسارات عربية آمنة.
- إدخال مستندات آمن مع مراجعة وتحويل PDF الممسوح إلى OCR.
- أدوات سياقية للجداول والصور.
- اختبارات صريحة لدورة حياة الدرس واستقلال مساري PDF.

## 11. المشكلات المفتوحة والمخاطر

- إعادة اختبار لصق زر الفأرة الأيمن؛ التحسينات موجودة لكن النجاح البصري 100% غير مؤكد.
- تحقق بصري كامل للتعديلات غير الملتزمة في شريط الإدراج ونافذة المعادلات على سطح المكتب واللوحي والهاتف.
- تحقق يدوي لـ PDF النص الحي بالعربية والمعادلات والنسخ/البحث.
- إعادة اختبار الطباعة الفعلية بسبب واقعة معاينة فارغة سابقة.
- اختبار OCR طرفيًا على ملفات ممسوحة متنوعة.
- ضغط حزم Vite كبير؛ التحسين لاحق ولا يسبق سلامة المحرر والبيانات.
- تخزين الصور Base64 في `localStorage` قابل لبلوغ الحصة؛ لا تستبدله سريعًا دون خطة ترحيل.
- لا توجد في الاختبارات الحالية تغطية UI بصرية آلية كاملة؛ النجاح البرمجي لا يكفي لاعتماد تصميم المحرر.

## 12. الملفات والمكونات الرئيسية

| الملف/المجلد | الدور |
|---|---|
| `src/App.tsx` | Shell، التنقل، الحالة العامة، الصلاحيات، فتح الدرس |
| `src/modules/curriculum/pages/CurriculumTreeView.tsx` | مستكشف المناهج التدريجي |
| `src/modules/lessons/pages/LessonEditorView.tsx` | دورة الدرس، المخطط، الحفظ، المعاينة، الطباعة، الاختصارات |
| `src/modules/editor/components/EditorPanel.tsx` | مخطط البطاقات ومحرر البطاقة المركز والتنقل |
| `src/modules/editor/components/RichTextEditor.tsx` | TipTap، التنسيق، الإدراج، اللصق، RTL، الصور والجداول |
| `src/modules/editor/components/EquationEditorModal.tsx` | محرر MathLive للمعادلات |
| `src/modules/editor/components/DocumentImportModal.tsx` | استيراد DOCX/PDF/HTML/TXT ومراجعته |
| `src/modules/editor/components/OcrImportModal.tsx` | تكامل OCR |
| `src/modules/editor/components/ImageNodeView.tsx` | سلوك الصورة داخل المحرر |
| `src/services/smartPasteEngine.ts` | تحليل/تنظيف/تحويل اللصق |
| `src/services/bidiContentPipeline.ts` | تطبيع المحتوى المختلط والاتجاه |
| `src/services/storage.ts` | التخزين، الترحيل، النسخ الآمن، التطبيع، الاستيراد/التصدير |
| `src/repositories/` | واجهة المستودعات ومحولات التخزين المحلي |
| `src/components/PrintPreviewModal.tsx` | نافذة المعاينة وأوامر الطباعة والتصدير |
| `src/components/UnifiedPreviewToolbar.tsx` | شريط المعاينة ومسارا PDF وPNG |
| `src/components/PaginatedA4Preview.tsx` | تقسيم وعرض A4 |
| `src/utils/pdfExporter.ts` | PDF كصورة |
| `src/utils/pdfV2Exporter.ts` | PDF نص حي |
| `src/utils/exportPreflightGate.ts` | تجهيز وتدقيق مشترك قبل التصدير |
| `src/tests/lessonLifecycleAudit.test.ts` | حواجز دورة حياة الدرس واستقلال PDF |
| `src/tests/previewExportAudit.test.ts` | فحص القوائم والمعادلات والطباعة |
| `src/index.css` | قواعد المحرر والمعاينة والطباعة/PDF؛ تعديلها واسع الأثر |

## 13. ملفات التسليم للوكيل التالي

هذا الملف مخصص للسياق المستقر ولا ينبغي تحديثه بعد كل جلسة. استخدم الملفات الثلاثة بهذا الترتيب:

1. `AI_PROJECT_CONTEXT.md`: الهدف والبنية والتقنيات والقرارات والتاريخ والحالة العامة.
2. `AI_RULES.md`: القواعد الدائمة والممنوعات وحماية البيانات والتحقق.
3. `CURRENT_TASK.md`: المهمة النشطة، التعديلات الحالية، نتائج الاختبارات، وما بقي.

## Handoff for Next AI Agent

اقرأ الملفات الثلاثة بالترتيب أعلاه، ثم افحص Git والكود الفعلي قبل التنفيذ. لا تكرر المعلومات اليومية هنا؛ حدّث `CURRENT_TASK.md` في نهاية كل جلسة، ولا تعدّل هذا الملف إلا عندما تتغير بنية المشروع أو قراراته الدائمة.

### قرار مساحة العمل — 2026-09-24
- `LessonOutline` هو عرض مشترك للمخطط المختصر والموسع، والإجراءات تستدعي callbacks القائمة في `EditorPanel`.
- آخر بطاقة تُحفظ في ذاكرة جلسة التطبيق حسب lessonId؛ لا تغيير لخدمة التخزين أو مخطط البيانات. إعادة تحميل التطبيق تبدأ من الأولى.
- حالات البطاقات تصف وجود المحتوى بحسب النوع ولا تعني جودة المحتوى أو صلاحية الطباعة.
- العودة بعد تحقق الحفظ تستخدم `onSavedBack` لتجنب فتح تحذير تغييرات قديم من closure سابق للحفظ.
