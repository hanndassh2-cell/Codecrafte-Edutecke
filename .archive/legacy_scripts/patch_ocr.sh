cat src/modules/editor/components/OcrImportModal.tsx | sed 's/onClick={() => setShowExecutionCenter(true)}/id="ocr-execution-center-btn" onClick={() => setShowExecutionCenter(true)}/' | sed 's/onClick={onClose}/id="ocr-close-btn" onClick={onClose}/' | sed 's/type="file"/id="ocr-file-upload-input" type="file"/' | sed 's/onClick={handlePasteFromClipboard}/id="ocr-paste-btn" onClick={handlePasteFromClipboard}/' | sed 's/onChange={(e) => setSubjectName(e.target.value)}/id="ocr-subject-input" onChange={(e) => setSubjectName(e.target.value)}/' | sed 's/onClick={handleSaveToQuestionBank}/id="ocr-save-btn" onClick={handleSaveToQuestionBank}/' | sed 's/onClick={handleRunOcr}/id="ocr-run-btn" onClick={handleRunOcr}/' | sed 's/onClick={handleSolve}/id="ocr-solve-btn" onClick={handleSolve}/' | sed 's/onClick={handleApply}/id="ocr-apply-btn" onClick={handleApply}/' > tmp_ocr.tsx
mv tmp_ocr.tsx src/modules/editor/components/OcrImportModal.tsx
