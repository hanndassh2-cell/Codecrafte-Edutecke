# EduTech — Permanent AI Rules

> قواعد دائمة وملزمة لأي وكيل ذكاء اصطناعي يعمل على المشروع.  
> اقرأ أيضًا `AI_PROJECT_CONTEXT.md` ثم `CURRENT_TASK.md` قبل أي تعديل.

## منهج العمل

1. افحص المشروع والكود الفعلي قبل اقتراح الحل؛ لا تعتمد على أسماء الملفات أو الذاكرة وحدها.
2. افحص `git status`, `git diff`, `git log` والفرع/HEAD قبل الكتابة.
3. التعديلات غير الملتزمة ملك للمستخدم: افهمها وحافظ عليها ولا تستبدلها أو تتراجع عنها.
4. لا تعِد بناء المشروع أو المحرر أو محرك المعادلات من الصفر. حسّن البنية الحالية بتغييرات صغيرة قابلة للمراجعة.
5. لا تعدّل إلا الملفات المرتبطة مباشرة بالمهمة الحالية، ولا تنفذ إصلاحات جانبية واسعة دون طلب.
6. تعامل مع الواجهة بعقلية الأستاذ الذي يدخل المحتوى يوميًا، ومع البيانات بعقلية مبرمج مسؤول عن عدم فقدانها.
7. البساطة والإفصاح التدريجي قاعدة: الأدوات اليومية ظاهرة، والمتقدمة سياقية أو داخل قوائم واضحة.

## حماية البيانات ودورة الحفظ

1. لا تغيّر مفاتيح التخزين أو نماذج `Lesson/Card/Question` أو إصدار المخطط أو SSOT دون ضرورة مثبتة.
2. أي تغيير بيانات يحتاج ترحيلًا idempotent، نسخة إنقاذ، تحقق كتابة، rollback، توافقًا مع البيانات القديمة، واختبارات.
3. لا تحذف حقولًا أو تنسيقات قديمة أو غير معروفة لمجرد أن TypeScript لا يعرفها.
4. لا تفترض أن Prisma هي قاعدة تشغيل نسخة الويب؛ راجع مسار التخزين الفعلي أولًا.
5. لا تعِد fallback إلى أول درس عند فقدان أو خطأ `lessonId`.
6. لا تسمح للإغلاق أو السابق/التالي أو المعاينة أو التصدير بتجاوز فشل الحفظ.
7. يجب أن تستخدم المعاينة والتصدير أحدث محتوى موجود في المحرر، لا نسخة حالة قديمة.
8. أي حذف مادي يحتاج هدفًا واضحًا وتأكيدًا مناسبًا، ولا يشمل بيانات أو ملفات لا تخص المهمة.

## ثوابت تجربة الاستخدام

1. المسار المعتمد هو: `المنهاج ← الدرس ← مساحة إعداد موحدة (المحرر والمخطط الجانبي) ← المعاينة ← التصدير/الطباعة`.
2. المنهاج هو نقطة الدخول لتحرير الدرس؛ لا تعِد شجرة المنهاج إلى شاشة إعداد الدرس.
3. مخطط الدرس يعرض بطاقات مختصرة للترتيب والتحكم؛ لا تعرض جميع المحررات مفتوحة داخله.
4. تحرير البطاقة يتم في مساحة مركزة شبيهة بمستند Word/Google Docs.
5. المعاينة نافذة كاملة عند الطلب؛ لا تعِد المعاينة الجانبية الدائمة.
6. يجب أن تبقى العربية وRTL هي الأساس، مع LTR تلقائي للمقاطع الرياضية والإنجليزية عند الحاجة.
7. لا تضف عشرات الأزرار إلى الشريط الأساسي؛ ضع الوظيفة في مجموعة إدراج أو قائمة أو شريط سياقي مناسب.
8. لا تعتبر الواجهة ناجحة بنجاح TypeScript فقط؛ افحصها بصريًا وتفاعليًا على Desktop/Tablet/Mobile.

## المحرر والمعادلات

1. محرر البطاقة هو أهم جزء في النظام؛ لا تقبل حلًا يضعف الكتابة أو الحفظ أو اللصق أو الإخراج.
2. حافظ على TipTap/ProseMirror وامتداداته الحالية ما لم يثبت سبب تقني قوي للتغيير.
3. محرك MathLive/KaTeX ناجح وقائم؛ لا تعِد بناءه. حسّن تنظيم الواجهة والتدفق فقط عند الإمكان.
4. اختبر المحتوى العربي والإنجليزي المختلط، القوائم، الصور، الجداول، والمعادلات inline/block.
5. اختبر اللصق من Word والويب بطريقتي `Ctrl/Cmd+V` وزر الفأرة الأيمن؛ لا تعلن إغلاق مشكلة الفراغات دون تحقق بصري.
6. الاستيراد أو OCR يجب أن يتيح المراجعة قبل الإدراج أو الاستبدال، وألا يكتب فوق المحتوى بصمت.

## المعاينة والتصدير والطباعة

1. أبقِ `src/utils/pdfExporter.ts` لمسار PDF كصورة و`src/utils/pdfV2Exporter.ts` لمسار PDF النص الحي مستقلين.
2. لا تدمج المسارين، ولا تجعل أحدهما fallback صامتًا للآخر.
3. لا تضف طبقة نص خفية إلى PDF الصورة؛ قد تفسد ترتيب العربية.
4. PDF الصورة هو المسار المعتمد للدقة البصرية، والنص الحي يحتاج تحققًا مستقلًا من العربية والنسخ والبحث.
5. اختبر المعاينة وPDF بنوعيه والطباعة بعد أي تغيير في CSS أو البطاقات أو الصور أو الجداول أو المعادلات.
6. تعديل `src/index.css` واسع الأثر؛ لا تستخدمه لحل مشكلة محلية قبل فحص البدائل المحدودة.

## التحقق وGit والتسليم

1. بعد التعديل شغّل على الأقل `npm run lint`.
2. قبل التسليم النهائي شغّل `npm test` و`npm run build`، وأضف اختبارًا عند تغيير البيانات أو دورة الحياة.
3. دوّن أي تحذير أو اختبار لم يُشغّل؛ لا تقل إن كل شيء ناجح إن لم يُفحص.
4. لا تنفذ commit أو push أو publish إلا إذا كانت المهمة تمنح ذلك صراحةً.
5. لا تلتزم ملفات `edutech-preview-*.tar.gz`؛ هي نواتج نشر محلية.
6. لا تنفذ أوامر Git مدمرة ولا تمسح تغييرات المستخدم.
7. قبل إنهاء كل جلسة حدّث `CURRENT_TASK.md`: ما تغير، ما اختُبر، ما بقي، الملفات المعدلة، وcommit/النشر إن حدث.
8. حدّث `AI_PROJECT_CONTEXT.md` فقط عند تغير بنية المشروع أو قرار معماري دائم؛ لا تستخدمه كسجل يومي.

